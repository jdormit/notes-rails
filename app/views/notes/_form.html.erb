<%= form_for @note do |f| %>
  <p>
    <%= f.label :title %>
    <br />
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :text %>
    <br>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to "back", notes_path %>

<% if @note.errors.any? %>
  <div>
    <h3><%= "Error".pluralize(@note.errors.count) %>:</h3>
    <ul>
      <% @note.errors.full_messages.each do |msg| %>
        <li>
          <%= msg %>
        </li>
      <% end %>
    </ul>
  </div>
<% end %>
